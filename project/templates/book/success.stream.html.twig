
<turbo-stream action="update" target="comment-length-refresh">
    <template>
        {# {{ include('book/_form.html.twig') }} #}
    </template>
</turbo-stream>

<turbo-stream action="append" target="comment-content-refresh">
    <template>
        <div>
            {% include 'book/_comment.html.twig', {comment, book: book, index: loop.index} %}
        </div>
    </template>
</turbo-stream>

<turbo-stream action="update" target="new-comment-form">
    <template>
        {{ include('comments/_form.html.twig', { form }) }}
    </template>
</turbo-stream>