<!DOCTYPE html>
<html class="dark:bg-gray-800 dark:text-white">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}GeekBook!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@meilisearch/instant-meilisearch/templates/basic_search.css" />
        <meta name="description" content="">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}   
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}   
        {% endblock %}
    </head>
    <body class="dark:bg-gray-800 dark:text-white">
        {% include './layout/navbar.html.twig' %}
        {% block body %}{% endblock %}
        {% include './layout/footer.html.twig' %}
        <script src="https://cdn.jsdelivr.net/npm/@meilisearch/instant-meilisearch/dist/instant-meilisearch.umd.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/instantsearch.js@4"></script>
        {# <script type="application/json" id="mercure-url">
            {{ mercure('https://gkbook.traefik.me/livre/{slug}')|json_encode(constant('JSON_UNESCAPED_SLASHES') b-or constant('JSON_HEX_TAG'))|raw }}
        </script>
        <script>
            const eventSource = new EventSource("{{ mercure([
                'https://gkbook.traefik.me/livre/{slug}',
                'https://gkbook.traefik.me/livre/{slug}'
            ])|escape('js') }}");

            eventSource.onmessage = event => {
                console.log(JSON.parse(event.data));
            }
        </script> #}
    </body>
</html>
